# Changelog

## Unreleased
- Show an explicit QA failure when a selected file cannot be decoded as an image instead of silently producing no result.
- Revoke local blob URLs on both successful and failed image loads.
- Add regression coverage for unreadable-image messaging.

## 1.0.0
- Initial release
